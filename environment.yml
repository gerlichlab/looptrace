# conda env create|update -f environment.yml
name: pychrtrace
dependencies:
    - python>=3.7
    - dask
    - pandas
    - numpy
    - scipy
    - scikit-image
    - zarr==2.4.0
    - pip
    - pip:
        - napari[all]
        - pyyaml
        - czifile
        - PySimpleGUI
        - aicsimageio