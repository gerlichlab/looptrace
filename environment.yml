# conda env create | update -f environment.yml
name: looptrace
dependencies:
    - python=3.8
    - scipy
    - pandas
    - dask
    - joblib    
    - pip
    - pip:
        - flowdec[tf_gpu]
        - scikit-image
        - scikit-learn
        - nd2
        - pyyaml
        - seaborn
        - pyserial
        - PySimpleGUI
        - zarr
        - napari[all]
        - fsspec>=0.9.0
        - imagecodecs
        - numcodecs
        - cellpose
        - tqdm
        - zarr