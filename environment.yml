# conda env create | update -f environment.yml
name: looptrace
dependencies:
    - python=3.8
    - scipy
    - pandas
    - dask
    - joblib    
    - pip
    - pip:
        - scikit-image
        - pyyaml
        - zarr
        - fsspec>=0.9.0
        - imagecodecs
        - numcodecs
        - cellpose
        - tqdm
        - zarr