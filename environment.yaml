# conda env create | update -f environment.yaml
name: looptrace
channels:
    - conda-forge
    - defaults
    - numba
dependencies:
    - python == 3.8.10
    - pandas
    - numba
    - llvmlite
    - scipy
    - numpy
    - pandas
    - dask
    - joblib
    - pip
    - pip:
        - tensorrt
        - scikit-image
        - pyyaml
        - zarr
          # for deconvolution
        - flowdec
        - fsspec >= 0.9.0
        - imagecodecs
        - napari
        - numcodecs
        - tqdm
        - cellpose
        - nd2
