# conda env create|update -f environment.yml
name: pychrtrace
dependencies:
    - python>=3.7
    - dask
    - joblib
    - pandas
    - numpy
    - scipy
    - scikit-image
    - zarr==2.4.0
    - plotly
    - nbformat
    - pip
    - pip:
        - napari[all]
        - pyyaml
        - czifile
        - PySimpleGUI
        - aicsimageio